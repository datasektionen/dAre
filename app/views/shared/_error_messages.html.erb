<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      Formuläret innehåller <%= object.errors.count %> fel.
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>