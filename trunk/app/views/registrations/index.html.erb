<div style="float: right; color: #EEEEEE;margin-bottom: -1px; font-size: 18pt; font-weight: bold;">
    ANMÄLDA DELTAGARE
</div>
<div class="tableclass">
<table class="table table-bordered">
    <thead class="head">
        <tr>
            <th>Förnamn</th>
            <th>Efternamn</th>
            <th>E-mail</th>
            <th>Årskurs</th>
            <th>Kommentar</th>
        </tr>
    </thead>
    <tbody class="registrations">

        <% @registrations.each do |registration| %>
        <tr class="<%= registration.hasPayedRegistration ? (registration.hasPayedTotal ? 'success' : 'info') : 'error'  %>">
            <td><%= registration.firstname %></td>
            <td><%= registration.surname %></td>
            <td><%= registration.email %></td>
            <td><%= registration.grade %></td>
            <td><a href="#" rel="tooltip" data-placement: "right" title="<%= registration.comment %>"><%= truncate(registration.comment, :length => 19, :omission => "...", :style =>"a:link{color: #000") %></a></td>
        </tr>
        <% end %>

    </tbody>
</table>
</div>
<%= will_paginate @registrations %>
<br />

<div style="float: right; color: #EEEEEE; font-size: 18pt; font-weight: bold;">
    RESERVER
</div>
<div class="tableclass">
<table class="table table-bordered">
    <thead class="head">
        <tr>
            <th>Förnamn</th>
            <th>Efternamn</th>
            <th>E-mail</th>
            <th>Årskurs</th>
            <th>Kommentar</th>
        </tr>
    </thead>
    <tbody class="registrations">

        <% @reserves.each do |registration| %>
        <tr class="<%= registration.hasPayedRegistration ? (registration.hasPayedTotal ? 'success' : 'info') : 'error'  %>">
            <td><%= registration.firstname %></td>
            <td><%= registration.surname %></td>
            <td><%= registration.email %></td>
            <td><%= registration.grade %></td>
            <td><a href="#" rel="tooltip" data-placement: "right" title="<%= registration.comment %>"><%= truncate(registration.comment, :length => 19, :omission => "...") %></a></td>
        </tr>
        <% end %>

    </tbody>
</table>
</div>

<br />

<script type="text/javascript">
    $("[rel=tooltip]").tooltip({placement: 'right'});
</script>